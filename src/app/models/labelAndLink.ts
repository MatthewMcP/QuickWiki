export class LabelAndLink {
Label: string;
HasLink: boolean;
constructor(
          label: string,
          hasLink: boolean){
            this.Label = label;
            this.HasLink = hasLink;
          }
}
